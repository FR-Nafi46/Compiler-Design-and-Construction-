kotha s = lo();

kotha target = "leadinguniversity";
kotha ans = "";

ongko j = 0;
ongko i = 0;

zotokkhon (i < s.size() && j < target.size()) {
    zodi (s[i] == target[j] || s[i] == target[j] - 32) {
        ans = ans + s[i];
        j++;
    }

    i++;
}

ko(ans);